I ran 7 AI agents on one M4 Mac Mini for 6 months.

One was failing silently the entire time. I found out from a user.

So I built a dashboard that shows you exactly what every agent is doing — alive, dead, drifting, burning tokens — in real time.

It's open source. It runs locally. No cloud.

Not launching yet. But if you run agents and want to see what they're actually doing, follow along.
